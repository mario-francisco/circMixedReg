% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_cmase_comparison_df.R
\name{prepare_cmase_comparison_df}
\alias{prepare_cmase_comparison_df}
\title{Compute CMASE_obs by group for multiple selectors and prepare data for plotting}
\usage{
prepare_cmase_comparison_df(
  theta,
  x,
  z,
  condition_levels,
  fit_fun_cv,
  fit_fun_boot,
  fit_fun_rot
)
}
\arguments{
\item{theta}{Vector of observed angular responses}

\item{x}{Matrix or data frame of continuous predictors}

\item{z}{Categorical variable (factor or character) for grouping}

\item{condition_levels}{Character vector of levels in z (e.g., sensory conditions)}

\item{fit_fun_cv}{Function to predict with bandwidth selector CV}

\item{fit_fun_boot}{Function to predict with bandwidth selector Bootstrap}

\item{fit_fun_rot}{Function to predict with bandwidth selector Rule-of-thumb}
}
\value{
Data frame with columns: Condition, Selector, CMASE
}
\description{
This helper function computes CMASE_obs by group for each bandwidth selector (CV, bootstrap, RoT)
and returns a tidy data frame suitable for plotting or table export.
}
\examples{
theta <- circular::circular(runif(30, 0, 2*pi))
x <- matrix(runif(30), ncol = 1)
z <- rep(c("A", "B", "C"), each = 10)
condition_levels <- c("A", "B", "C")
fit_dummy <- function(x, z) circular::circular(rep(pi, length(x)))
prepare_cmase_comparison_df(theta, x, z, condition_levels,
                             fit_dummy, fit_dummy, fit_dummy)
}
