% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_circular_surface.R
\name{plot_circular_surface}
\alias{plot_circular_surface}
\title{Plot Circular Regression Surface for Two Continuous Predictors}
\usage{
plot_circular_surface(
  x_grid,
  y_grid,
  z_matrix,
  title = "Circular Regression Surface",
  xlab = "Predictor 1",
  ylab = "Predictor 2"
)
}
\arguments{
\item{x_grid}{Numeric vector. Grid values for the first continuous predictor (e.g., actual distance).}

\item{y_grid}{Numeric vector. Grid values for the second continuous predictor (e.g., distance error).}

\item{z_matrix}{Numeric matrix. Estimated circular responses (in radians),
of size \code{length(y_grid)} x \code{length(x_grid)}.}

\item{title}{Optional string. Main title of the plot.}

\item{xlab}{Optional string. Label for the x-axis.}

\item{ylab}{Optional string. Label for the y-axis.}
}
\value{
A \code{ggplot2} object displaying a circular-valued surface as a heatmap.
}
\description{
This function visualizes the estimated circular regression function
over a two-dimensional grid of continuous predictor values.
The angular output is encoded using a perceptually uniform circular color scale.
}
\details{
It is intended for use with circular nonparametric regression models
involving two continuous covariates and one categorical factor (held fixed).
The resulting heatmap reveals how directional responses vary across
the joint domain of the two predictors.
}
\examples{
x_grid <- seq(-1, 1, length.out = 50)
y_grid <- seq(-1, 1, length.out = 50)
m_fun <- function(x, y) atan2(sin(pi * x * y), cos(pi * x * y)) \%\% (2 * pi)
m_grid <- outer(x_grid, y_grid, Vectorize(function(x, y) m_fun(x, y)))
plot_circular_surface(x_grid, y_grid, m_grid,
                      xlab = "Predictor 1",
                      ylab = "Predictor 2",
                      title = "Synthetic Circular Surface")
}
